# Real contact data. Addresses belonging to actual people - never publish.
# Written by scripts/jobs/export_app.py into the Vite public/ directory, so
# both `npm run dev` and `npm run build` pick it up without an extra step.
app/public/data.js
data/
drafts/
inputs/*
!inputs/profile.example.md
state/
CONTACTS.md
NEEDS_YOU.md

# Superseded prototypes. today.html embeds the real contact list inline.
viewer/

# Personal strategy and operating docs.
CLAUDE.md
PLAN.md
SCHEDULE.md
RUNBOOK.md
STATUS.md
.claude/

app/node_modules/
app/dist/
app/*.tsbuildinfo

.idea/
__pycache__/
*.pyc
.DS_Store
sync_preview.sh
